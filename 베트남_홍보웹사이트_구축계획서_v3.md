# 2026 집중지원기업 베트남 현지 홍보 웹사이트 구축 계획서 (v3)

가치 중심 콘텐츠 · 3개국어(베/영/한) · GitHub 완결 배포 · 관리자 페이지 단일 운영 경로 · 기업 PDF 자료 임베드

작성일: 2026년 7월 3일 (v1: 7/1, v2: 7/3)
기초 데이터: **신규 마스터 시트 — 집중지원기업 통합 리스트 (66개사, 7개 산업분야)**
https://docs.google.com/spreadsheets/d/1BFzo8DYo73qkqH3KvxmK9oXh0hhxbl12B0ukHkThVRM/

---

## 0. 버전 변경 이력

### v3 변경 (이번 개정)

| # | 변경 | 내용 |
|---|---|---|
| 1 | **기초 데이터 소스 교체** | 구 'Pool' 폴더(47개사, 설문응답 기반) → **신규 마스터 시트(66개사)**. 시트에 이미 '베트남 AX/DX 기여 가능성' 컬럼이 문제→가치 서사로 정리되어 있어 v2의 가치 중심 스키마와 정합성이 높고, 초기 콘텐츠 가공량이 크게 준다. 참가 행사·SDGs 컬럼이 추가되어 스키마에 신규 필드로 반영 |
| 2 | **바이어 마스킹의 위상 변화** | 신규 시트에는 희망 바이어·담당자 개인정보 컬럼이 **없다**. 따라서 최초 구축 단계의 마스킹 작업은 사라지고, 마스킹 규칙은 이후 관리자 경로로 '희망 협력분야'를 수집할 때 적용하는 입력 규칙으로만 유지 |
| 3 | **기업 PDF 자료 임베드 기능 신설** | 기업이 제출한 회사·제품소개서 PDF를 상세 페이지에 임베드 뷰어로 게시. 업로드는 관리자 경로(Issue Form 첨부)에서 수행 (4.3절·7장·9장) |

### v2 변경 요약 (v1 대비)

Google API 파이프라인 전면 삭제(최초 1회 수동 구축 + 관리자 경로 단일화), GitHub Pages·Issue Forms 기반 GitHub 완결 스택(시크릿 0개), 가치 중심(문제→해결→가치) 콘텐츠 스키마, 베트남어 기본 언어, 다크 글래스모피즘 디자인 시스템(8장), "공개 불가 정보는 시스템에 입력하지 않는다" 원칙.

---

## 1. 개요 및 프로젝트 범위

### 1.1 목적

베트남의 기업·기관이 한국 ICT 기업의 솔루션을 "내 문제를 해결해 줄 수 있는 선택지"로 인식하고 관심을 갖게 만들어, NIPA 호치민IT지원센터를 통한 상호 연결(매칭 문의)로 이어지게 한다. 모든 기업 콘텐츠는 **① 어떤 문제를 → ② 어떻게 해결하며 → ③ 어떤 가치를 주는가** 순서의 서사로 구성하며, 기업 연혁·조직 소개형 문구는 사용하지 않는다.

### 1.2 확정된 운영 조건

| 항목 | 확정 내용 |
|---|---|
| 표시 언어 | 베트남어(기본) · 영어 · 한국어 — URL 언어 코드 방식, 루트 접속 시 /vi/로 안내 |
| 콘텐츠 최초 구축 | **신규 마스터 시트(66개사)를 1회성으로 활용.** API·서비스 계정 없이 담당자가 시트를 내려받아 Claude 지원 하에 66개사 콘텐츠 JSON 초안을 생성하고 사람이 검수 |
| 이후 콘텐츠 운영 | 관리자 경로(GitHub Issue Forms → 자동 PR → 검수·승인 → 배포) 단일화. **기업 PDF 자료 업로드 포함** |
| 배포 | GitHub 저장소 → Actions → GitHub Pages. 외부 호스팅·API 키·유료 서비스 없음 |
| 본 문서의 범위 | 구축 계획 문서. 실제 코드 구현은 승인 후 후속 단계 |

### 1.3 핵심 원칙

- GitHub 위에 올라가는 모든 것(콘텐츠 JSON·PDF·Issue·PR)은 **처음부터 대외 공개 가능한 정보만** 담는다. 검토·마스킹은 시스템에 입력하기 *전에* 내부에서 완료한다.
- 바이어 실명, 담당자 개인정보는 어떤 형태로도 시스템에 입력하지 않는다.
- 모든 게시물(PDF 포함)은 입력자가 아닌 별도 승인자의 PR 승인(프리뷰 확인 포함)을 거쳐야 반영된다.

---

## 2. 원본 데이터: 신규 마스터 시트 (최초 1회 구축 전용)

### 2.1 시트 구성과 스키마 매핑

신규 마스터 시트는 66개사 × 9개 컬럼으로 구성되며, 컬럼 대부분이 5장 스키마에 직접 매핑된다. 특히 '베트남 AX/DX 기여 가능성' 컬럼은 이미 "베트남의 현황·문제 → 솔루션의 기여" 구조로 작성되어 있어 v2에서 가장 공수가 클 것으로 예상했던 가치 서사 작업의 원문이 된다.

| 시트 컬럼 | 스키마 필드 | 가공 방법 |
|---|---|---|
| No. / 기업명 | company_id / name | 분야코드-일련번호 채번, 국문·영문 병기 분리 |
| 분야 | sector | 7개 분야 그대로 (사이버보안·제조DX·의료헬스케어바이오·클라우드인프라·환경에너지스마트시티·AI데이터분석·교육콘텐츠플랫폼) |
| 참가 행사 | events | **신규 필드.** "8월 InnoEx", "9월 방문단" 등 — 상세 페이지 배지 + "현장에서 만나기" CTA 소재 |
| 기업소개 | (내부 참고) | 연혁 중심 문장은 그대로 쓰지 않고, tagline·problem 도출의 참고 자료로만 사용 (1.1 원칙) |
| 홈페이지 | official_website | URL 정규화(프로토콜 보정, 오탈자 점검 — 예: 일부 행에 특수문자 혼입 확인됨) |
| 기업 제품/솔루션 | value_summary + key_metrics | [솔루션명]-설명 구조를 솔루션 블록으로 분해. 본문에 포함된 수치(정확도 99.3%, 검사시간 60% 단축, AUROC 0.961 등)를 key_metrics로 추출 |
| 베트남 AX/DX 기여 가능성 | **problem + vietnam_fit** | 앞부분(베트남 현황·문제 서술) → problem, 뒷부분(기여·정책 정합성) → vietnam_fit으로 분리 |
| SDGS 해당여부 | sdgs | **신규 필드.** SDG 번호 배열로 정규화 — 베트남 정부·기관 독자에게 유효한 신뢰 신호로 상세 페이지에 배지 표시 |

### 2.2 보조 소스와 제외 대상

- 구 'Pool' 폴더의 기업별 회사·제품소개서 PDF는 **로고 확보와 PDF 임베드 원본(7장) 후보**로만 보조 활용한다. 시트와 기업 목록이 다르므로(47 vs 66) 텍스트 소스로는 쓰지 않는다.
- 'AI보고서' 폴더(CONFIDENTIAL 내부 전략 문서)는 v1·v2와 동일하게 어떤 단계에서도 참조하지 않으며, 초기 구축 Claude 세션에도 업로드하지 않는다.

### 2.3 구축 방법

Google 서비스 계정·API를 쓰지 않는다. 담당자가 시트를 xlsx로 내려받아 Claude 세션에 업로드하고, 2.1의 매핑 규칙에 따라 66개사 × 3개 언어 JSON 초안을 생성한 뒤 검수하여 저장소에 최초 커밋한다. 이 작업은 1회만 수행하며, 이후 시트와 웹사이트 간 연동은 존재하지 않는다 — **최초 커밋 이후에는 저장소의 콘텐츠 JSON이 유일한 원본(Single Source of Truth)이다.**

---

## 3. 협력 희망 정보(바이어) 처리 원칙

신규 마스터 시트에는 희망 바이어·담당자 개인정보 컬럼이 없으므로 **최초 구축 단계에서 마스킹 작업은 발생하지 않는다.** `target_buyer_display`는 초기에는 분야별 표준 문구("현지 파트너 매칭은 KICC HCMC를 통해 문의")로 채우고, 이후 기업별 희망 협력분야가 수집되는 대로 관리자 경로에서 갱신한다. 이때 다음 입력 규칙을 적용한다.

- 실명 바이어·담당자 정보는 애초에 시스템(Issue·PR·JSON)에 입력하지 않는다. 업종·유형으로 일반화한 문구만 입력한다(예: 통신사 실명 → "현지 통신·ICT 기업", 병원 실명 → "현지 대형 종합병원").
- Issue Form의 해당 필드 안내문에 일반화 예시 매핑표를 상시 게시한다.
- 2차 안전장치로 `validate.js`(CI)가 이메일·전화번호 정규식과 내부 관리 차단어 목록(해시 보관)을 점검해 위반 시 PR을 실패 처리한다.
- 원본 실명 정보가 필요한 내부 업무는 Drive 내부 문서에서만 관리한다.

---

## 4. 데이터 구축·운영 아키텍처

### 4.1 전체 흐름

**[최초 1회]** ① 마스터 시트 확보 → ② Claude 지원 표준화·가치 서사 초안(2.1 매핑) → ③ 번역 초안(vi/en) → ④ 사람 검수 → ⑤ 저장소 최초 커밋 → ⑥ 빌드·배포

**[운영]** ① 관리자 경로 입력(Issue Form, PDF 첨부 포함) → ② Actions가 콘텐츠 JSON·PDF 파일 브랜치와 PR 자동 생성 → ③ PR 프리뷰에서 승인자 확인 → ④ 병합 → ⑤ 자동 빌드·배포

### 4.2 관리자 경로 — 기업 정보 등록·수정

- **입력 UI = GitHub Issue Forms.** `.github/ISSUE_TEMPLATE/company.yml`에 5장 스키마의 필드를 구조화 폼으로 정의. GitHub 로그인만으로 사용하며 별도 CMS·인증 인프라·API 키가 없다.
- 제출 시 intake Action이 폼을 파싱해 `content/{lang}/companies/{id}.json`을 생성·수정하는 브랜치와 PR을 자동 생성한다. 기존 `company_id` 기입 시 수정, 공란 시 신규(분야별 다음 일련번호 자동 채번).
- **입력자·승인자 분리:** branch protection으로 셀프 승인을 금지하고, 승인자는 PR 프리뷰 화면(4.4절)에서 실제 렌더링을 확인 후 승인한다.
- **삭제 = 소프트 삭제.** '공개 여부'를 비공개로 제출하면 `is_published: false`. 파일 삭제는 제공하지 않는다.

### 4.3 관리자 경로 — 기업 PDF 자료 업로드 (신설)

기업이 제출한 회사소개서·제품소개서·브로슈어 PDF를 상세 페이지에 게시하는 기능이다. 업로드부터 게시까지 기존 검수 관문을 그대로 통과한다.

- **업로드 방법:** Issue Form의 'PDF 자료' 필드(textarea)에 파일을 드래그 앤 드롭하면 GitHub이 첨부 URL을 자동 삽입한다. 제목·언어(vi/en/ko)를 함께 기입한다. 기업당 여러 건 등록 가능.
- **intake Action의 처리:** 첨부 URL에서 파일을 내려받아 ① MIME 타입이 PDF인지 ② 크기가 10MB 이하인지 ③ 파일명 정규화(`assets/docs/{company_id}/{slug}.pdf`)를 수행한 뒤 콘텐츠 PR에 함께 커밋한다. 검증 실패 시 PR 코멘트로 사유를 알리고 체크를 실패 처리한다.
- **게시 전 확인 사항(폼 안내문에 명시):** 해당 PDF가 ⓐ 기업이 대외 배포용으로 제공한 자료인지 ⓑ 웹 게시에 동의했는지. 내부용 자료·CONFIDENTIAL 표기 자료는 업로드 금지 — 공개 저장소이므로 커밋되는 순간 공개된다(1.3절).
- **교체·삭제:** 같은 슬롯에 재업로드하면 교체 PR이 생성된다. 게시 중단은 해당 문서 항목을 폼에서 '비공개'로 제출(JSON에서 제외 — 파일은 이력상 남으므로, 애초에 공개 가능 자료만 올린다는 원칙이 전제다).
- **용량 관리:** PDF는 저장소 용량을 소모하므로 기업당 총 20MB·사이트 전체 700MB를 예산 상한으로 두고, 초과 조짐 시 업로드 전 압축(안내문에 무료 압축 방법 게시)을 요청한다. Git LFS는 GitHub Pages가 서빙하지 못하므로 사용하지 않는다.

### 4.4 검수 화면 (PR 프리뷰)

pr-preview 계열 Action으로 PR마다 `…/pr-preview/pr-{번호}/`에 임시 배포한다. 승인자는 이 URL에서 문구·번역·**PDF 뷰어 렌더링**을 눈으로 확인하고 승인한다. PR 병합·종료 시 프리뷰는 자동 삭제.

---

## 5. 콘텐츠 데이터 모델

모든 기업은 아래 스키마로 3개 언어(vi/en/ko) 버전을 갖는다. 저장소의 모든 필드·파일은 공개 가능 정보다.

| 필드 | 설명 | 비고 |
|---|---|---|
| company_id | 분야코드-일련번호 | URL slug 겸용 |
| name (vi/en/ko) | 기업명 | 국문·영문 병기 분리 |
| logo_image | 로고 (기업에 원본 PNG/SVG 직접 요청) | |
| sector | 7개 산업분야 중 1 | 시트 '분야' |
| **events** | 참가 행사 배열 — 예: `{name: "InnoEx", date: "2026-08"}` | **신규.** 시트 '참가 행사'. 배지 + "현장에서 만나기" CTA |
| tagline (vi/en/ko) | 한 줄 가치 제안 | |
| problem (vi/en/ko) | 베트남 기업·기관이 겪는 문제 1~2문장 | 시트 'AX/DX 기여' 앞부분 |
| value_summary (vi/en/ko) | 솔루션 블록(이름+설명)과 제공 가치 | 시트 '제품/솔루션' |
| key_metrics [3] | `{label, value}` 3개 — 시트 본문 수치 추출(정확도·단축률·도입처 수 등), 없으면 인증·수상 등 정성 지표 | 3분할 그리드(8장) |
| vietnam_fit (vi/en/ko) | 베트남 적용 포인트·정책 정합성 | 시트 'AX/DX 기여' 뒷부분 |
| **sdgs** | SDG 번호 배열 — 예: `[9, 11]` | **신규.** 시트 'SDGS'. 배지 표시 |
| official_website | 공식 홈페이지 URL | CTA 외부 링크 |
| target_buyer_display (vi/en/ko) | 희망 협력분야 (3장 규칙, 초기엔 표준 문구) | |
| **pdf_documents []** | `{title(vi/en/ko), file, lang, is_published}` — 임베드 게시용 PDF 목록 | **신규.** 4.3절 경로로만 추가 |
| review_status / last_edited_by / last_edited_at / is_published | 운영 메타 (렌더링 제외) | |

---

## 6. 콘텐츠 작성 가이드

1. **문제 먼저.** 첫 문장은 독자의 문제로 시작한다. 신규 시트의 'AX/DX 기여' 컬럼이 대체로 이 구조를 이미 갖추고 있으므로, 초안 생성 시 이 컬럼을 서사의 골격으로 삼는다.
2. **가치는 수치로.** key_metrics 3개를 시트 본문에서 우선 추출하고(예: 에이트테크 "선별 정확도 99.3%", 디플리 "검사시간 60% 단축", 웨이센 "선종발견율 +11%"), 수치가 없는 기업은 로고 요청과 함께 직접 요청한다.
3. **베트남 맥락 명시.** vietnam_fit에 적용 대상·정책 정합성(PDP8, Decree 53/2022, SMEdx 등 시트에 이미 포함된 근거)을 1문장 이상 유지한다.
4. **CTA는 연결로.** 최종 행동은 KICC HCMC 매칭 문의(hcmc@nipa.kr)로 수렴. events가 있는 기업은 "8월 InnoEx에서 직접 만나보세요" 형태의 보조 CTA를 추가한다.
5. 검수 우선순위는 vi > en > ko. 한국어 원문 확정 → AI 초벌 → Trang/Tram 현지 검수.

---

## 7. 사이트 구조

| 경로 | 내용 |
|---|---|
| /{lang}/ (기본 /vi/) | 히어로(8장) + 산업분야 진입 + 하이라이트 기업 + 다가오는 행사(InnoEx·방문단) 섹션 |
| /{lang}/sector/{slug} | 분야별 기업 카드 리스트 (로고, tagline, key_metric 1개, 행사·SDG 배지) |
| /{lang}/company/{id} | 상세 — ① 히어로(로고·기업명·분야·행사 배지·tagline) ② 문제(problem) ③ 해결과 가치(value_summary + key_metrics 3분할) ④ 베트남 적용(vietnam_fit + SDG 배지) ⑤ **자료실(PDF 임베드 뷰어)** ⑥ 희망 협력분야 ⑦ CTA(공식 웹사이트 / 매칭 문의 / 행사에서 만나기) |
| /{lang}/directory | 전체 검색·필터(분야·SDG·행사 참가 여부) — 정적 JSON + 클라이언트 JS, 66개사 규모에 충분 |
| /{lang}/about | 센터 소개, 문의 창구 |

**PDF 임베드 표시 방식(⑤ 자료실):** 문서 목록(제목·언어 태그) 중 선택 시 페이지 내 뷰어로 표시한다. 기본 구현은 `<iframe>`/`<object>` 네이티브 뷰어로 하되, 모바일 브라우저(특히 iOS Safari)의 인라인 PDF 표시가 불안정하므로 화면 폭 기준으로 모바일에서는 **썸네일 카드 + "새 탭에서 보기/다운로드" 버튼**으로 자동 전환한다. 필요 시 2단계 개선으로 PDF.js 뷰어(정적 배포 가능, 외부 API 불필요)를 도입한다.

문의 폼: `mailto:hcmc@nipa.kr` + 제목·본문 프리필(기업명 자동 삽입). 언어 전환: /vi/·/en/·/ko/ 독립 정적 빌드 + 상단 스위처.

---

## 8. 디자인 시스템

*(v2와 동일 — 변경 없음. 요약: 차콜 #1E1E1E 베이스, 포인트 컬러 1개(소프트 민트 #5FE3C0 권장), 화이트 투명도 3단 위계, Be Vietnam Pro + Pretendard, 떠 있는 히어로 카드(radius 24px) + muted 배경 영상 루프(재생/일시정지 토글, reduced-motion 폴백), 좌 텍스트/우 글래스 카드(세그먼트 탭 + 3분할 통계 그리드), 하단 신뢰 지표 바, Primary 민트 rounded-lg + 화살표 / Secondary 고스트.)*

v3 추가 사항만 기술한다.

- **신뢰 지표 바 갱신:** `66+ Companies · 7 Sectors · Supported by NIPA · VI / EN / KO`
- **행사 배지:** 카드·상세 히어로에 소형 배지(고스트 스타일, --text-mid) — 예: `InnoEx · Aug 2026`. 포인트 컬러는 쓰지 않는다(포인트 1색 원칙 유지).
- **SDG 배지:** 공식 컬러 아이콘 대신 모노톤 아웃라인 배지(번호+명칭)로 통일해 다크 모노톤 원칙을 지킨다.
- **PDF 뷰어 프레임:** 글래스 카드와 동일한 표면(--bg-elev, radius 16px, --glass-border)으로 감싸고, 문서 목록 탭은 히어로 세그먼트 탭과 동일 컴포넌트를 재사용한다.

---

## 9. 기술 스택

| 영역 | 채택 | 비고 |
|---|---|---|
| 사이트 생성 | Astro (정적, i18n) | |
| 호스팅 | GitHub Pages | public 저장소, 시크릿 0개 (GITHUB_TOKEN 내장) |
| PR 프리뷰 | pr-preview 계열 Action | Pages 하위 경로 |
| 관리자 경로 | GitHub Issue Forms + intake Action | 기업 정보 + PDF 첨부 처리(4.3절) |
| **PDF 처리** | intake Action: MIME·10MB 검증, 경로 정규화, 커밋. 표시: iframe/object + 모바일 다운로드 폴백(→ 필요 시 PDF.js) | **Git LFS 금지**(Pages 미지원). 압축 안내로 용량 관리 |
| 검증 CI | validate.js — 개인정보 정규식, 차단어, is_published 정합성, PDF 크기·타입 | 위반 시 병합 차단 |
| 번역 | Claude 초벌 + Trang/Tram 검수 | 번역 API 불필요 |
| 이미지 | 로고 원본 직접 요청, WebP 변환 | |

**저장소 공개 범위:** public 기본(Pages 무료 요건). 콘텐츠 JSON·PDF·Issue·PR이 모두 공개되므로 1.3절 원칙이 전제다.

---

## 10. GitHub 저장소 구조

```
vn-promo-site/
├── content/
│   ├── vi|en|ko/companies/{company_id}.json
├── assets/
│   └── docs/{company_id}/{slug}.pdf        # 4.3절 PDF (공개용 자료만)
├── site/                                    # Astro (8장 디자인 토큰)
├── scripts/
│   ├── issue-to-content.js                  # 폼 → JSON + PDF 커밋
│   └── validate.js
├── .github/
│   ├── ISSUE_TEMPLATE/company.yml           # 기업 정보 + PDF 첨부 폼
│   └── workflows/
│       ├── company-intake.yml
│       ├── pr-preview.yml
│       └── deploy.yml
└── README.md                                # 운영 매뉴얼
```

`main` 보호 브랜치: 직접 push 금지, PR + 1인 이상 승인, 셀프 승인 금지 — 승인·병합 시에만 배포.

---

## 11. 프로젝트 일정 (안, 8주)

| 주차 | 단계 | 주요 작업 |
|---|---|---|
| 1주 | 초기 데이터 구축 | 마스터 시트 확보, Claude 지원 66개사 JSON 초안(2.1 매핑), **게시 동의·로고·공개용 PDF·보완 수치 요청 발송(크리티컬 패스)** |
| 2주 | 콘텐츠 검수 | problem/value/metrics 문안 확정, URL 정규화, 검수자 교차 확인 |
| 3~4주 | 사이트 구축 | Astro 템플릿 + 8장 디자인 구현, PDF 뷰어(모바일 폴백 포함) |
| 4~5주 | 관리자 경로 구축 | Issue Form(PDF 첨부 포함)·intake Action·validate CI·PR 프리뷰·branch protection |
| 5~6주 | 번역 | vi/en 초벌 + 현지 검수, 용어집 확정 |
| 7주 | QA | 개인정보·링크·PDF 렌더링(모바일) 전수 점검, 입력→승인→배포 리허설 |
| 8주 | 오픈 | Pages 배포, 운영 매뉴얼 전달, 미회신 기업 "준비중" 처리 |

66개사로 늘었지만 신규 시트가 이미 가공된 서사·수치를 담고 있어 초안 공수는 v2(47개사, 설문 원문 가공) 대비 오히려 줄어들 것으로 판단, 총 기간은 8주를 유지한다.

---

## 12. 리스크 및 유의사항

1. **게시 동의·자료 확보 (크리티컬 패스)** — 66개사의 동의·로고·공개용 PDF·보완 수치를 1주차부터 병행 요청. 미회신 기업은 "준비중" 또는 PDF·수치 없이 우선 게시.
2. **공개 저장소 정보 유출** — Issue·PR·커밋 이력까지 공개. 입력 전 검토 완료 원칙(1.3) + validate.js + 폼 안내문 3중 장치. **특히 PDF는 내부용·CONFIDENTIAL 자료가 실수로 올라가지 않도록 폼에서 공개용 여부 체크를 필수화**하고, 실수 커밋 시 이력 정리 절차를 매뉴얼에 포함.
3. **PDF 저작권·버전** — PDF는 각 기업 제작 자료이므로 웹 게시 동의를 개별 확보하고, 기업이 개정판을 보내면 교체 경로(4.3절)로 갱신한다.
4. **저장소 용량** — PDF 누적으로 저장소가 비대해지면 클론·빌드가 느려진다. 파일 10MB·기업 20MB·전체 700MB 상한과 압축 안내로 관리하고, 상한 근접 시 외부 오브젝트 스토리지 전환을 별도 검토한다(현 단계에서는 도입하지 않음).
5. **모바일 PDF 렌더링** — iOS Safari 인라인 표시 불안정 → 7장 폴백 규칙을 QA에서 실기기로 검증.
6. **대외비 문서 오용 방지** — 'AI보고서' 폴더 자료는 초기 구축 세션 포함 어디에도 사용 금지.
7. **영상 성능** — 히어로 영상 6MB 상한, 포스터 우선 로드.
8. **데이터 기준** — 최초 구축은 신규 마스터 시트를 단일 기준으로 삼고, 커밋 이후에는 저장소 JSON이 유일한 원본이다. 구 47개사 자료와의 정합성은 관리 대상이 아니다.

---

## 13. 다음 단계 (Action Items)

1. 66개사 대상 게시 동의 + 로고 원본 + **공개용 PDF 소개서** + 보완 수치 요청 발송 — 1주차 즉시
2. 시트 → 스키마 매핑(2.1) 기준의 파일럿 3개사(예: 이글루코퍼레이션, 웨이센, 에이트테크 — 수치가 풍부한 기업) JSON 초안 생성 → 서사 품질 확인
3. 포인트 컬러 확정 및 배경 영상 후보 선정
4. GitHub 저장소(public) 생성, branch protection·협업자 권한 설정
5. Issue Form 문안(협력분야 일반화 힌트, PDF 공개용 체크 필수 항목 포함) 확정, 입력자·승인자 지정
6. 파일럿 기업 상세 페이지 + 히어로 + PDF 뷰어 프로토타입 제작 → 피드백 반영

---

## 부록. 사전 준비 체크리스트

| # | 필요 사항 | 비고 |
|---|---|---|
| 1 | GitHub 저장소 생성·협업자 초대 | public |
| 2 | branch protection (승인 필수, 셀프 승인 금지) | |
| 3 | GitHub Pages 활성화 (+ 선택: 커스텀 도메인) | 시크릿 불필요 |
| 4 | 66개사 게시 동의·로고·공개용 PDF·수치 확보 | 크리티컬 패스 |
| 5 | 로고·배경 영상·PDF 저작권 확인 | |
| 6 | 검수자(승인자) 2인 이상 지정 — Trang/Tram 포함 | GitHub 계정 |
